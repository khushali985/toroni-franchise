[phases.setup]
nixPkgs = ["nodejs", "php", "php81Packages.composer"]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm install"
]

[phases.build]
cmds = [
  "npm run build",
  "php artisan config:cache"
]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"